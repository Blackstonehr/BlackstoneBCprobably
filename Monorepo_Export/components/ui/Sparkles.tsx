
import { palette } from '../../system/palette';

// UI PRIMITIVE: Sparkles (Particle engine)
// Uses system palette for colors

export const Sparkles = () => {
  return (
    <div style={{ color: palette.colors.bone }}>
      {/* Sparkle logic here */}
      Sparkles
    </div>
  );
};
